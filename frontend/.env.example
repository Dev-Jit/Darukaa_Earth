# Backend API base URL (no trailing slash). Production: https://<your-render-service>.onrender.com
VITE_API_BASE_URL=http://localhost:8001

# Mapbox public access token (client-side maps on project detail)
# Create at https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=
